import AuthCard from "../_components/AuthCard"

const ResetPassword = () => {
  return (
    <main className="w-full h-screen flex flex-col items-center justify-center">
      <AuthCard mode="Reset password" />
    </main>
  )
}

export default ResetPassword
