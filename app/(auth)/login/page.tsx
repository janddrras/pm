import AuthCard from "@/app/(auth)/_components/AuthCard"

const Login = () => {
  return (
    <main className="w-full h-screen flex flex-col items-center justify-center">
      <AuthCard mode="Log in" />
    </main>
  )
}

export default Login
